//Global Objects
    console.log();  // GLOBAL SCOPE :Can be accessed anywhere to log anything on console.
    setTimeout(); // Used to call a function after a delay.
    clearTimeout();
    setInterval(); //Repeatedly call a function after a given delay
    clearInterval(); //Stop a function from being called repeatedly
    window // In browser all global objects can be accessed using window object but in Node window doesn't work
    global // In Node we use global instead of window to access global objects
    